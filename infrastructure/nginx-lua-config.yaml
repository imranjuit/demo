apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-lua-scripts
  namespace: ingress-nginx
data:
  404-redirect.lua: |
    -- No logic needed here unless you want
    -- to extend functionality later
    -- This loads successfully via init_by_lua_block
    return {}

